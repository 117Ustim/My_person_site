# Session state

Дата: 2026-09-12
Статус: Восстановлена визуальная и временная логика главной страницы по оригинальному сайту; production build и smoke-test пройдены.

## Сделано

- Убран iframe и старый runtime-слой snapshots: приложение работает через Next.js App Router.
- Все 47 маршрутов сайта сохранены в `lib/site-content.ts` и статически генерируются.
- Страницы переведены на настоящие React-компоненты: маркетинговые страницы, страницы аудиторий, integrations, integration detail, security, pricing, about, media и юридические статьи.
- Общие визуальные блоки вынесены в отдельные папки с соседними CSS Modules: `MarketingHero`, `FeatureSlider`, `FeatureGrid`, `FaqSection`, `LogoStrip`, `BookingCta`, `PageSection`, `IntegrationDirectory`, `IntegrationDetail`, `ArticlePage`, `LegalIndex`.
- `dangerouslySetInnerHTML`, `lib/snapshot.ts`, `SnapshotPage`, `SnapshotSection`, `SnapshotInteractions`, `public/sitegrab/pages` и `public/sitegrab/index.html` удалены.
- Все используемые изображения и логотипы находятся локально в `public/sitegrab/assets`; неиспользуемые варианты выгрузки, tracker-бинарники и compatibility CSS удалены.
- Интерактивный `FeatureSlider` сохранён: автоматическое переключение, клики по карточкам, fade изображений и видимая белая progress-line через `requestAnimationFrame`.
- FAQ, фильтр/поиск интеграций, header dropdown, mobile menu и cookie banner работают через React state.
- Сохранены metadata, canonical URLs, `loading`, `error`, `not-found`, sitemap, robots и favicon.
- PDF-архив `public/legal` удалён ранее по запросу; dead PDF routes также убраны из карты маршрутов.
- Восстановлена геометрия hero на главной: заголовок, CTA, центральный preview и камни по краям теперь используют реальные локальные AVIF-ассеты оригинала.
- Восстановлены карточки `Save Time & Grow AUM` через компонент `ModuleShowcase` с отдельным CSS Module и локальными фоновыми/передними изображениями.
- Восстановлен блок `The platform that scales your firm`: заголовок и описание левой колонки, порядок элементов, размеры изображения и интервалы приведены к оригиналу.
- В `FeatureSlider` восстановлены автоматическое переключение, fade-смена изображения и белая progress-line: таймер запускается при появлении блока во viewport через `IntersectionObserver`, линия рисуется через `requestAnimationFrame`.
- С оригинального сайта в `public/sitegrab/assets` добавлены точные ассеты hero, камней и двух showcase-карточек.
- Восстановлен внутренний `gap: 26px` между верхней линией и содержимым каждого пункта `FeatureSlider`; из-за этого меню больше не сжимается и не проваливается вниз.
- На главной восстановлен второй блок `Who it's for`: добавлен полноценный `Consolidators`-слайдер с изображением слева, меню справа и пунктом `Everything for Independent Firms and more`.
- `FeatureSlider` получил режим `reverse` для зеркальной раскладки блоков без дублирования компонента.
- Правый камень в hero главной заменён на портрет из `/Users/ustim/Desktop/НЕ УДАЛЯТЬ/Фото/2026-08-11 22.57.24.jpg`.
- Для портрета удалён белый фон с сохранением реального альфа-канала; финальный локальный asset — `public/sitegrab/assets/hero-founder-transparent-graphite-v2.png`.
- Восстановлен прежний фирменный вариант портрета: графитовый фильтр, затемнение под камень и мягкий тёплый акцент слева; desktop/mobile позиционирование сохранено в `MarketingHero.module.css`.
- Промежуточные дубликаты изображения удалены после проверки, в приложении оставлен только используемый финальный asset.
- Предыдущая промежуточная попытка заменить левый камень на спутниковый 3D-глобус была отменена после визуальной сверки; затем задача возвращена в работу по новому референсу wireframe-глобуса.
- Обработанный правый портрет `public/sitegrab/assets/hero-founder-transparent-graphite-v2.png` восстановлен; натуральная цветная версия в приложение не подключалась.
- Неиспользуемый старый вариант `hero-founder-transparent-v4.png` удалён после переключения на восстановленный фирменный asset.
- Левый камень главного hero заменён на отдельный клиентский WebGL-компонент `EarthGlobe` на Three.js. Глобус выполнен в wireframe-стилистике нового референса: тёмная низкополигональная сфера, сферическая сетка и локальные географически корректные контуры материков из Natural Earth GeoJSON.
- Для глобуса заданы осевой наклон Земли `23,5°`, плавное вращение по часовой стрелке, направленное освещение и однотонная непрозрачная заливка материков без береговой обводки. Материки строятся UV-маской CanvasTexture из Natural Earth GeoJSON, поэтому заливка занимает всю поверхность каждой формы, а не только её край. Атмосферный fresnel-ореол удалён; при `prefers-reduced-motion` глобус отображается неподвижно.
- Частота сферической сетки выровнена по референсу: меридианы и параллели идут с шагом `20°`; стартовый ракурс смещён к Африке, чтобы узнаваемые контуры были видны сразу после загрузки.
- Геометрия глобуса адаптирована отдельно: на desktop он остаётся левым крупным объектом за preview, а на mobile выводится выше центрального preview, чтобы не перекрываться им и оставаться читаемым.
- Камера глобуса отодвинута до `z: 5.05`, поэтому сфера полностью помещается внутри canvas без плоского среза сверху/снизу; wrapper дополнительно клипует canvas по круглому контуру. Desktop-размер ограничен `420px`, mobile — `50vw`.

## Проверки

- `npx tsc --noEmit` — успешно.
- `npm run build` — успешно, 53 Next.js entries сгенерированы: 47 страниц и служебные маршруты.
- Production smoke-test: все 47 маршрутов вернули `200`.
- В отрендеренном HTML нет `dangerouslySetInnerHTML`, `data-snapshot-section` и ссылок на старые HTML snapshots.
- Проверены локальные изображения, найденные в HTML: 44 URL, битых загрузок — 0.
- Визуально проверены hero, CTA, камни по краям, каталог интеграций и progress-line слайдера в production preview на `3001`.
- Проверка анимации в браузере: до появления слайдера `transform: scaleX(0)` и ширина линии `0px`; в видимом состоянии линия растёт по треку `460px`, после цикла активный пункт переключается автоматически.
- Все 47 маршрутов из `lib/site-content.ts` в production preview вернули `200`.
- Проверка исходников: нет `console.log`/`console.warn`/`console.error`/`console.table`, `!important`, `dangerouslySetInnerHTML` и старых `data-snapshot-section`.
- Сверка геометрии меню с оригиналом: активный пункт `79px`, неактивные пункты `47px`, расстояние от описания до меню `149px`.
- Статичные `AudiencePreview` на главной удалены: в актуальной структуре оригинала блок `Who it's for` содержит полноценный слайдер `Consolidators`; полноценный `FeatureSlider` также присутствует на `/whos-it-for/independent-firms`.
- Проверка второго домашнего слайдера с оригиналом: заголовок `Who it's for` — `top: 3338`, изображение и заголовок `Consolidators` — `top: 3476`, изображение `872×756`, меню справа `460px`; активная линия заполняется и переключает пункт на `Rapid onboarding`.
- Проверка hero после восстановления фирменной обработки в production preview: изображение загружается через `next/image`, прозрачность подтверждена (`PNG`, `1254×1254`, `4` канала, `hasAlpha: true`), runtime-ошибок нет.
- После финальной подмены повторно пройдены `npm run build`, `npx tsc --noEmit` и smoke-test всех 47 маршрутов — все вернули `200`.
- После промежуточной отмены спутниковой версии production build повторно завершился успешно: 53 Next.js entries; TypeScript и smoke-test всех 47 маршрутов также прошли.
- Для hero главной восстановлен оригинальный общий слой нижнего затемнения: градиент поверх preview, левого камня и правого портрета начинается с `656px`, выходит за нижний край на `42px` и достигает `#000` на `83.35%`; исходная цветокоррекция изображений не менялась.
- Правый портрет в hero поднят на `50px` через `transform: translateY(-50px)`; изменение применяется и на мобильном layout.
- Правый портрет в hero дополнительно уменьшен примерно на `6%`: desktop `76.4vw × 47.9vw`, mobile `62vw × 43%`; выравнивание, подъём и обработка сохранены.
- Центральная hero-карточка интерфейса уменьшена примерно на `5%`: desktop `64vw` (до `1084.8px`), mobile `122vw`; пропорции, центрирование и нижнее затемнение сохранены.
- После перехода на wireframe-версию успешно пройдены `npx tsc --noEmit` и `npm run build`: сгенерированы 53 Next.js entries. Визуально проверен desktop hero со свежим референсом: canvas отрисовывается, сплошные материки видимы, глобус не скрывается за central preview; ошибок Three.js/WebGL нет.
- После замены hero-текста на `Перетворюю ідеї на готові цифрові продукти` повторно пройдены браузерная визуальная проверка, `npx tsc --noEmit`, `git diff --check` и `npm run build`; production-сборка сгенерировала 53 маршрута.
- Design-QA по второму пользовательскому референсу сохранён в `design-qa.md`: desktop capture `/tmp/implementation-wireframe-globe-final.png`, mobile capture `/tmp/implementation-wireframe-globe-mobile.png`; оба viewport проверены, итог `final result: passed`.
- Финальный desktop preview проверен после уменьшения и исправления камеры: `/tmp/implementation-globe-small-final.jpg`, viewport `1695×887`, canvas `420×420` (`x:34`, `y:445`), внутренний WebGL-буфер `630×630`; круглый контур и континенты отрисовываются без runtime-ошибок.
- После настройки частоты сетки и стартового ракурса повторно проверен кадр `/tmp/implementation-globe-grid20-africa.jpg`: сетка `20°`, Африка и соседние контуры читаются, осевой наклон сохранён.
- После выравнивания UV-развёртки карта материков совпадает с географической сеткой: при старте в центре читается Африка, а не смещённая на 90° Америка; повторная проверка выполнена в preview `3000`.
- По утверждённому первому варианту глобус переведён в палитру камня из референса: графитовая основа, тёплые серо-каменные материки и приглушённая серо-коричневая сетка; освещение и roughness настроены под матовую фактуру без дополнительного свечения.
- Внутри маски материков добавлены заметный каменный градиент, детерминированное зерно и несколько редких неровных медно-коричневых прожилок; цвет оставлен в одной серо-каменной гамме, а CanvasTexture переведена в sRGB, чтобы оттенки не высветлялись браузером.
- После финальной правки заливки/ореола проверен кадр `currentGlobeTab` в preview `3000`: ореол отсутствует, все материки непрозрачно залиты одним цветом, береговых линий нет; консоль чистая.
- После исправления геометрии заливки проверен кадр `/tmp/implementation-globe-solid-land.jpg`: UV-маска заполняет материки полностью изнутри, цвет единый, обводок и ореола нет, сетка `20°` остаётся видимой поверх сферической поверхности; консоль чистая.
- После отмены последнего действия глобус возвращён к предыдущему варианту внутренних оттенков: материки остаются цельными и читаемыми, границы без обводки, градиент, зерно и редкие медные прожилки ограничены маской; консоль чистая.
- Для солнечной подсветки добавлен тёплый направленный ключевой свет сверху-слева, общий ambient снижен, а правый контровой свет ослаблен; проверочный кадр `/tmp/implementation-globe-sun-left.jpg` показывает мягкое освещение левой полусферы без ореола.
- Убраны фасеточные полосы плохой графики: сфера и слой материков переведены на smooth shading, геометрия остаётся отдельной от меридианов; карта материков осветлена, а левый ключевой свет усилен. Проверочный кадр `/tmp/implementation-globe-smooth-sun.jpg` показывает чистую гладкую поверхность и заметную подсветку слева.
- По новому референсу фактура материков переведена на локальный asset `/sitegrab/assets/0739-about-stone.CIU_KC6y_Z1peJd0-c9c65b3996.webp`: освещённая серо-графитовая плоскость, медные прожилки и тёмные трещины накладываются внутри географической маски; проверочный кадр `/tmp/implementation-globe-reference-stone.jpg`.
- Каменная фактура стала базовой заливкой каждой географической маски: все материки и острова получают один и тот же stone pattern, собранный из плотного участка asset без прозрачных полей, а градиент используется только как безопасный fallback при недоступном asset. На вращении проверены разные долготы, включая кадр `/tmp/implementation-globe-all-continents-texture-rotated.jpg`; консоль чистая.
- Исправлены ошибки `Cannot find module './331.js'` и `Cannot find module './611.js'`: причиной были три одновременно работающих `next dev`, конфликтовавших за `.next`. Процессы остановлены, кэш `.next` очищен, запущен один dev-сервер на `3000`; свежий preview отдаёт `200`, глобус отображается, консоль без runtime-ошибок.
- После ручного полного оборота на `localhost:3000` проверены 16 кадров (`/tmp/globe-dense-audit-00.jpg`…`15.jpg`, контактный лист `/tmp/globe-dense-full-contact.jpg`): все видимые материки и острова получают одну плотную каменную фактуру без плоских прозрачных провалов. Для паттерна используется непрозрачный внутренний crop локального asset `0739-about-stone`, поэтому на освещённой и теневой стороне сохраняется единый рельеф; текущий финальный кадр — `/tmp/implementation-globe-dense-final.jpg`.
- После повторного полного оборота уже после production-сборки проверены 16 кадров (`/tmp/globe-polished-audit-00.jpg`…`15.jpg`): фактура сохраняется на всех показавшихся долготах, а в теневых кадрах остаётся только естественное затемнение без артефактных полос. Начальный светлый flash устранён: слой материков имеет `opacity: 0` до готовности карты и включается после загрузки GeoJSON и stone asset.
- По запросу уменьшен масштаб каменной структуры: tile стал `320px` вместо `720px`, фильтр фактуры и дополнительные overlay-слои ослаблены. Благодаря единому паттерну в координатах всей UV-карты все материки используют одинаковый мелкий рельеф; свежий крупный кадр — `/tmp/globe-fine-texture-crop.jpg`, полный аудит — `/tmp/globe-fine-audit-00.jpg`…`15.jpg`.
- Фактура переработана в общем шейдере `components/EarthGlobe/stoneMaterial.ts`: GeoJSON теперь служит только альфа-маской, а один и тот же плотный crop исходного asset накладывается трипланарно по координатам сферы. Это убирает растяжение по bounding box материков, сохраняет одинаковый texel density и скрывает швы зеркальным повторением; проверено на полном 360° цикле из 16 кадров `/tmp/stone-final-360-00.jpg`…`15.jpg` и крупном изолированном глобусе.
- Финальный desktop кадр после production-сборки сохранён в `/tmp/implementation-globe-uniform-final.jpg`: главная открывается на `localhost:3000`, Африка и соседние острова покрыты общей мелкой stone-фактурой, карточка hero и портрет не затронуты. `npm run build` снова завершился успешно, временный QA-маршрут удалён.
- Главный hero-заголовок заменён на украинский текст `Перетворюю ідеї на готові цифрові продукти`; для кириллицы включён serif-fallback `Times New Roman`, чтобы сохранить характер Ivory вместо автоматической подстановки Arial. Desktop-перенос проверен в браузере: две строки, прежний размер и ритм.
- По запросу главный hero-заголовок переведён на системный стек iPhone (`-apple-system`, `SF Pro Display`, `SF Pro Text`); остальные заголовки, размеры, переносы и композиция не менялись. В рабочем preview заголовок проверен в две строки.
- По результатам типографического сравнения системный стек заменён только у главного hero-заголовка на локальный `Source Serif 4 Variable`: украинская кириллица поддерживается, включён оптический размер, вес `400`, перенос сохраняется в две строки; Ivory и Inter в остальных частях сайта не менялись.
- Типографическая правка проверена после production-сборки: `npx tsc --noEmit`, `git diff --check` и `npm run build` (53 маршрута) проходят; preview снова работает на `localhost:3000`.
- Подзаголовок главного hero заменён на `Full-stack розробник. Сайти, CRM-системи та мобільні застосунки для реальних бізнес-завдань`: используется тот же `Source Serif 4 Variable`, размер увеличен с 14px до 28px, перенос визуально проверен на две строки.
- Подзаголовок опущен через отступ `56px`, CTA-кнопка под ним отключена только для домашнего hero; остальные hero-кнопки сайта сохранены.
- Центральная hero-карточка использует локальный однотонный dashboard VetScanCT `public/sitegrab/assets/vetscanct-dashboard-anna-14-uniform.png`: приветствие «Ана», дата «14 вересня», пропорции `1586 / 992`; mobile-позиция `top: 67%` предотвращает перекрытие подзаголовка.
- Hero-карточка VetScanCT — прозрачный графитовый preview без светлых пятен: фон `rgba(15, 16, 17, 0.3)`, `1px` ободок, мягкий внутренний блик, радиус `18–28px`, без backdrop blur, прозрачность содержимого `74%`; desktop/mobile проверены без overflow и перекрытия текста.
- Нижний переход домашнего hero переработан в двухчастный fade: затемнение внутри hero заканчивается чёрным, а дополнительный слой в верхней части LogoStrip продолжается вниз и постепенно становится прозрачным; desktop и mobile проверены без горизонтального overflow.
- Подпись над технологиями на главной заменена с `Made by the people behind` на `Технології, з якими я працюю`; в preview проверен точный украинский текст.
- По запросу весь сайт переведён на единый `Source Serif 4 Variable`: глобальный body, навигация, заголовки, кнопки и карточки используют один шрифт с поддержкой украинской кириллицы; подключения Inter и Ivory удалены из layout.
- Подпись `Технології, з якими я працюю` получила системный стек `SF Pro Text`/`SF Pro Display` через `-apple-system`; на macOS используется установленный SF Pro без копирования системного файла в репозиторий.
- Подзаголовок главного hero `Full-stack розробник...` переведён на тот же системный стек SF Pro; размеры и переносы сохранены, в браузере проверен точный текст.
- На главной вместо логотипов добавлена бесшовная движущаяся лента из 9 технологий: `React`, `Next.js`, `TypeScript`, `Node.js`, `React Native`, `Swift`, `Firebase`, `PostgreSQL`, `GitHub`; на desktop одновременно видны 5, иконка стоит перед названием.
- По запросу лента технологий на desktop уменьшена до ширины `800px`, компактный шаг между технологиями сохранён, а список продолжает двигаться бесшовно без горизонтального overflow.
- Убрана лишняя горизонтальная линия за краями карточки меню: закрытая `.dropdownBar` теперь не занимает высоту и имеет прозрачный фон, открытие dropdown сохранено.
- Верхняя CTA-кнопка в шапке переименована в `Обговорити проєкт`; под описанием hero добавлена кнопка `Переглянути роботи`, ведущая якорем к секции проектов `#projects`. Проверены desktop/mobile-размеры, горизонтального overflow нет.
- Для CTA-кнопок добавлен отдельный системный стек SF Pro: он используется у `Обговорити проєкт` в шапке и `Переглянути роботи` в hero; основной шрифт сайта не изменён. Computed-style проверен в desktop preview.
- Тексты hero `Перетворюю ідеї на готові цифрові продукти` и `Full-stack розробник...` подняты на 20px: desktop padding сверху `140px`, mobile `106px`; проверены позиции и отсутствие горизонтального overflow.
- Подзаголовок hero `Full-stack розробник...` дополнительно поднят на 5px уменьшением верхнего отступа с `56px` до `51px`; desktop-позиция проверена (`top: 363px`), overflow отсутствует.
- Кнопка `Переглянути роботи` опущена на 3px: её верхний отступ изменён с `20px` на `23px`; desktop-позиция проверена, горизонтального overflow нет.
- По согласованной концепции старый блок `Save Time & Grow AUM` заменён только на homepage-секцию `Що я створюю`: добавлен компонент `CapabilityShowcase` с тремя направлениями, украинскими описаниями, технологиями и временными локальными визуалами. Desktop — асимметричная сетка из большой и двух компактных карточек, mobile — одна колонка.
- Карточки `CapabilityShowcase` выполнены в текущей стилистике сайта: прозрачный графитовый фон, тонкий ободок, скругление, мягкое затемнение изображений, оранжевая нумерация и лёгкое hover-движение. Якорь `#projects` получил прокрутку с учётом фиксированной шапки; старые секции не изменялись. Проверены `npx tsc --noEmit`, `git diff --check`, desktop/mobile preview.
- По обратной связи ступенчатая раскладка `CapabilityShowcase` переработана в 4 равные карточки: `01/02` сверху, `04/03` снизу; правая верхняя и левая нижняя смещены вниз на `24px`. Добавлено направление `Серверна частина та інфраструктура`, desktop-колонки выровнены, mobile-смещения сброшены. Проверены desktop/mobile preview и отсутствие overflow.
- По эскизу пользователя карточки `CapabilityShowcase` заменены на две асимметричные «ступенчатые» пары: у верхней карточки большой визуал слева и тонкий текстовый блок справа, у нижней — зеркально. Так размещены все 4 направления, поэтому исчезла пустая левая зона. Сохранены временные локальные изображения, прозрачный графитовый фон, тонкий ободок и мягкие скругления; на mobile каждая карточка складывается в цельный вертикальный блок. Проверки: `npx tsc --noEmit`, `git diff --check`, desktop 1440px и mobile 390px — ошибок и горизонтального overflow нет.
- После уточнения карточки полностью переделаны: теперь каждая из четырёх карточек — одна цельная масштабируемая SVG-форма с общим фоном и непрерывным ободком, а не два наложенных прямоугольника. Пары `01/02` и `03/04` складываются как тетрис без визуального пересечения: между горизонтальными частями оставлен зазор `16px`, между вертикальными ступенями — отдельный зазор; все внешние и вогнутые углы имеют плавные радиусы. На ширине до `900px` карточки переходят в цельную вертикальную форму. Desktop-секция и mobile проверены в браузере, полный QA сохранён в `design-qa.md`.
- Стык фотографии и текстовой части в `CapabilityShowcase` смягчён фоном текста: графитовая полоса заходит на изображение на `40px` по всей высоте SVG-карточки и плавно становится прозрачной. Для зеркальных карточек направление градиента разворачивается; на mobile эффект отключён, так как бокового стыка там нет. Проверены desktop/mobile, `npx tsc --noEmit`, `git diff --check`; актуальный отчёт сохранён в `design-qa.md`.
- После обнаружения видимой границы отдельная 40-px полоса удалена. Теперь край фотографии растворяется альфа-маской: `80px` перехода находятся перед стыком и ещё `16px` уходят под текстовую часть, поэтому сквозь изображение проявляется точный полупрозрачный фон карточки без цветового шва. Для правых изображений маска зеркальная, на mobile отключена. Desktop/mobile и сравнительный кроп проверены; `design-qa.md` обновлён.
- Пропорции тетрис-карточек унифицированы: верхняя и нижняя карточки используют одинаковое деление `55%` под фотографию и `45%` под текстовый хвост. Нижний SVG-контур перестроен как зеркало верхнего, обе карточки получили одинаковую ширину и симметричное выравнивание по краям секции. При desktop-проверке обе фотографии имеют размер `696×404px`, оба хвоста — `556px` шириной и `154px` высотой по контуру; mobile остаётся одной колонкой без overflow.
- Обводка `CapabilityShowcase` переведена с серо-белого на тот же коралловый оттенок, что и номера карточек: `rgba(255, 135, 122, 0.42)`. Цвет применён к desktop SVG-контру и mobile border; геометрия и прозрачность карточек не изменены. Desktop/mobile проверены, `design-qa.md` обновлён.
- Внутри каждой визуальной части `CapabilityShowcase` добавлен `visualFrame` с рамкой `4px`: фотография вложена внутрь и остаётся центрированной, а рамка использует тот же `--surface-fill`, что и текстовая область. Затемнение перенесено внутрь рамки, плавная альфа-маска сохранена; desktop/mobile проверены через временный preview на `3002`, затем временный сервер остановлен.
- По последнему уточнению рамка вокруг фото удалена: `visualFrame` теперь даёт равномерный внутренний отступ `20px`, а `imageViewport` показывает вытянутое изображение с радиусом `18px` без собственного ободка. Фон вокруг фото и текстовый фон используют один `--surface-fill`, поэтому карточка выглядит единой; альфа-переход, тетрис-форма и коралловый внешний контур сохранены. Desktop (`656×364px` фото внутри `696×404px` visual) и mobile (`301×200px`) проверены без overflow; QA обновлён в `design-qa.md`.
- По дополнительной правке отступы вокруг фото подтверждены равными `20px` со всех сторон. Полностью сняты затемнение `imageViewport`, `opacity: 0.86` и боковые CSS-маски: фото отображается с исходной яркостью (`opacity: 1`, `filter: none`, `mask-image: none`). Фон визуальной части остаётся тем же `--surface-fill`, что и текстовая область; desktop/mobile проверены без overflow, QA обновлён.
- Для исключения расхождения справа отступ вокруг фото переведён на явный `padding: 20px` у `.visual`, а `.visualFrame` растягивается внутри него. В обеих зеркальных карточках DOM-проверка показала одинаковые отступы `20/20/20/20px`; desktop и mobile не имеют overflow, фото остаётся без затемнения.
- После уточнения по внешней рамке desktop-фото со стороны внутреннего изгиба уменьшено на `16px` через `width: calc(100% - 16px)` и закреплено у внешнего края визуальной области: у верхней пары запас добавлен справа, у нижней — зеркально слева. Это компенсирует изгиб SVG-контура и оставляет до него фактические `20px`; mobile сбрасывает компенсацию и сохраняет `20px` со всех сторон. Desktop/mobile проверены, QA обновлён.
- Исправлено различие фона внутри `CapabilityShowcase`: причина была в двойном наложении одинакового полупрозрачного `--surface-fill` на `.surface` и `.visual`. Фон `.visual` сделан прозрачным, поэтому фото-зона и текстовая зона теперь используют один слой карточки; desktop/mobile проверены без overflow, QA обновлён.
- По запросу весь интерфейс переведён на единый айфоновский шрифт SF Pro: глобальный `body`, заголовки, навигация, кнопки, карточки, статьи, служебные и внутренние страницы используют один системный стек `-apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display", sans-serif`. Подключение Source Serif 4 удалено из layout и точечных CSS-правил, неиспользуемая зависимость удалена из package-файлов; desktop/mobile и внутренняя страница проверены без overflow, production build на 53 маршрута успешен, QA обновлён.

## Текущий этап — i18n

- Добавлен клиентский i18n-провайдер в `lib/i18n.tsx` с языками `uk`, `ru`, `en`; украинский используется по умолчанию.
- В шапке добавлен компактный переключатель `UA / RU / EN` между навигацией About и CTA «Обговорити проєкт»; для mobile добавлен тот же control внутри меню.
- Переводы подключены к header, footer, cookie-баннеру, CTA, главной странице, маркетинговым страницам, интеграциям, legal/article-страницам и accessibility-текстам.
- Выбор языка сохраняется в `localStorage`, а `html[lang]` синхронизируется с активной локалью.
- Проверки: `npx tsc --noEmit` и `npm run build` прошли; production build сгенерировал 53 маршрута. Визуально проверены desktop, tablet без overflow и mobile menu на ширине 390px.
- После уточнения позиции переключатель на desktop получил дополнительный правый отступ и визуально центрируется между навигацией «Про нас» и CTA; desktop/mobile-варианты разделены, чтобы переключатель внутри мобильного меню не скрывался.
- Устранена ошибка dev-runtime `Cannot find module './611.js'`: остановлен старый процесс Next.js, очищен только сгенерированный `.next`, запущен один новый dev-сервер; smoke-test главной страницы вернул `200`.
- Блок `TechnologyMarquee` на главной поднят на `15px` целиком: подпись, бегущая лента и fade перемещаются синхронно без изменения размеров.
- По дополнительной правке блок поднят ещё на `20px`; итоговое смещение от исходной позиции составляет `35px`.
- Расстояние между `TechnologyMarquee` и заголовком «Що я створюю» уменьшено на `20px` через нижний padding блока; аналогичное уменьшение задано для mobile.
- Логотип объединён в единый знак `AU studio`: рядом с существующим `AU` добавлен прозрачный `public/assets/studio-logo-transparent.png`, компонент `BrandLogo` обновлён для шапки и футера, подпись доступности переведена через i18n. PNG проверен как RGBA с alpha-каналом, локальный smoke-test страницы вернул `200`.
- По референсу `AU` и `studio` немного уменьшены, а общий логотип выровнен по нижней линии через `align-items: flex-end`; размеры скорректированы отдельно для шапки и футера.
- По дополнительной правке знак `AU studio` уменьшен ещё примерно на 10%, нижнее выравнивание и пропорции сохранены.
- По следующей правке знак уменьшен ещё примерно на 10%; пропорции и нижняя линия выравнивания сохранены.
- Объединённый знак `AU studio` поднят на `4px` вверх целиком через transform, внутреннее выравнивание элементов не изменено.
- Знак поднят ещё на `2px`; итоговое смещение составляет `6px` вверх.
- Знак опущен на `1px`; итоговое смещение скорректировано до `5px` вверх.
- В `CapabilityShowcase` увеличен ступенчатый зазор между карточками на `20px` (`bottomCard: 190px`, высота пары `594px`), на mobile gap увеличен до `24px`; обводка карточек заменена на светло-серую `rgba(247, 245, 240, 0.3)`.
- Горизонтальный зазор карточек выровнен с вертикальным: ширина карточки теперь учитывает `20px` вместо `16px`, mobile gap также установлен `20px`.
- Обводка карточек сделана светлее: opacity светло-серого цвета повышена с `0.3` до `0.45` для desktop SVG-контура и mobile border.
- Заголовки карточек `CapabilityShowcase` переведены в тот же коралловый цвет `#ff877a`, что и номера `01/02`.
- Блок метаданных карточек (`01`, `02`, категория `ВЕБ` и аналоги) опущен на `2px` через transform; заголовки и описания не изменялись.
- Блок метаданных опущен ещё на `2px`; итоговое смещение составляет `4px` вниз.
- По запросу эксперимент со спутниками вокруг глобуса полностью удалён: восстановлены прежние размеры и позиции глобуса на desktop/mobile, удалены орбиты, иконки и связанные i18n-ключи.
- Для переключателя языков добавлена анимация «магнитная капля»: белая капсула плавно перемещается к выбранному языку за `460ms`, слегка растягивается в направлении движения и даёт краткий коралловый отблеск. Текст меняет тон независимо от капсулы; для `prefers-reduced-motion` остаётся мгновенный спокойный переход. Проверены TypeScript, `git diff --check`, desktop-переключение `UA → RU` и отсутствие горизонтального overflow.
- Белая активная капля переключателя заменена на прозрачную стеклянную линзу без изменения корпуса: добавлены полупрозрачные градиенты, светлая кромка, внутренний блик, `backdrop-filter` и объёмная нижняя тень. Коралловый отблеск убран; существующие движение и растяжение на `18%` сохранены. В браузере подтверждено промежуточное растяжение при `EN → RU`, итоговая круглая форма и отсутствие overflow; TypeScript и `git diff --check` проходят.
- Анимация стеклянной линзы сделана плавнее: длительность движения и деформации увеличена с `460ms` до `560ms`, easing смягчён до `cubic-bezier(0.16, 1, 0.3, 1)`, переход цвета текста увеличен до `240ms`, максимальное горизонтальное растяжение повышено с `18%` до `24%`. В браузере подтверждены `0.56s`, пиковый `scaleX(1.24)`, возврат к круглой форме и отсутствие overflow; TypeScript и `git diff --check` проходят.
- В `EarthGlobe` добавлена спокойная 3D-сеть проектов отдельным модулем `projectNetwork.ts`: 9 малых узлов на desktop и 5 на mobile, постоянный коралловый узел в координатах Киева, последовательные маршруты раз в `7s`, пологие приподнятые Bézier-дуги высотой `7%` радиуса, белый импульс и коралловое кольцо прибытия. После доставки на `1,7s` появляется один из четырёх CanvasTexture-значков — web, CRM, mobile или cloud; значки закреплены у поверхности и скрываются глубиной сферы. При reduced motion остаются только статичные узлы. Desktop проверен в полном hero и крупным планом: дуга и значок читаются, контент не перекрывают, runtime-ошибок и overflow нет; `npx tsc --noEmit`, `git diff --check` и проверка на debug-код проходят.
- Исправлен планировщик 3D-маршрутов: перед каждым запуском он прогнозирует вращение глобуса на весь активный интервал и проверяет глубину начала, конца и девяти участков дуги относительно камеры. Запускается только маршрут, который останется на передней стороне до появления значка; при отсутствии кандидата анимация ждёт, а последний маршрут не повторяется, если доступна альтернатива. В браузере последовательно подтверждены первый маршрут из Украины, второй на видимой стороне Азии и третий после ожидания над Америкой; runtime-ошибок и overflow нет, TypeScript и `git diff --check` проходят.
- В используемом PNG-превью CRM `vetscanct-dashboard-anna-14-uniform.png` заголовок точечно заменён с `Вітаємо у вашому дні, Ана` на `Вітаємо у вашому дні!`. Исходные размер `1586×992`, RGB-формат и весь интерфейс вне области заголовка сохранены; pixel-diff ограничен прямоугольником `803×140–932×219`, визуальная проверка полного изображения пройдена.
- Для домашнего hero добавлен отдельный класс русского заголовка: при локали `ru` ширина увеличивается с `960px` до `1100px`, поэтому «Превращаю идеи в готовые цифровые продукты» переносится в две строки. Украинская и английская версии используют прежнюю ширину. В браузере подтверждены русский заголовок высотой `172px` при `line-height: 86px` и украинская ширина `960px`; `npx tsc --noEmit` и `git diff --check` прошли.
- У кнопки `Обговорити проєкт` в шапке добавлена коралловая внутренняя обводка `#ff877a`, совпадающая с цветом заголовков карточек. Обводка сделана через `inset box-shadow`, поэтому размеры, скругление и расположение кнопки не изменились; в браузере подтверждены `177.42×36px`, радиус `999px` и нужный цвет. `npx tsc --noEmit` и `git diff --check` прошли.
- Заголовок блока с будущими работами заменён на `Вибрані проєкти`, добавлен подзаголовок `Реальні цифрові продукти для бізнесу — від CRM-систем і вебсайтів до мобільних застосунків`; для RU и EN добавлены i18n-переводы `Избранные проекты` / `Selected projects` и соответствующие описания. В браузере подтверждено отображение обоих украинских текстов; текущий контент слайдера не изменялся.
- Первый пункт блока работ обновлён: вместо `Незалежні фірми` теперь `CRM-системи та автоматизація`, описание заменено на `Цифрові системи для керування клієнтами, записами, оплатами, задачами та внутрішніми процесами бізнесу.`. Добавлены отдельные RU/EN-переводы; в браузере подтверждены украинский, русский и английский варианты, `npx tsc --noEmit` и `git diff --check` прошли.
- Второй пункт блока работ обновлён: вместо `Консолідатори` теперь `Вебсайти та лендинги`, описание — `Від лаконічних сайтів до складних вебзастосунків — цифрові рішення для бізнесу, експертів і персональних брендів.`. Добавлены отдельные RU/EN-переводы; в браузере подтверждены все три локали, `npx tsc --noEmit` и `git diff --check` прошли.
- Заголовок `Для кого` над вторым блоком работ удалён вместе с отдельным контейнером и его `80px` нижнего отступа; сам блок `Вебсайти та лендинги` и описание сохранены. В браузере подтверждено отсутствие `h2` внутри секции и корректное положение контента; `npx tsc --noEmit` и `git diff --check` прошли.
- После блока `Вебсайти та лендинги` добавлен третий блок `Мобільні застосунки` с описанием про iOS/Android и существующим мобильным интерфейсом `0605-ai-chat...webp`. Новый `FeatureSlider` использует обычную компоновку: текст слева, изображение справа, как в CRM-блоке; добавлен i18n-label для RU/EN. В браузере подтверждён порядок CRM → Web → Mobile, нужные стороны компоновки и визуальный размер `872×756px`; `npx tsc --noEmit` и `git diff --check` прошли.
- В секции `Мобільні застосунки` меню расширено до четырёх пунктов: `Мобільний кабінет`, `Швидкі дії`, `Розумні функції` и `Персональний досвід`. Для каждого пункта добавлен отдельный существующий визуальный ассет; названия, описания и alt-тексты переведены через i18n для UA/RU/EN. Сохранена общая анимация `FeatureSlider`: раскрытие активного описания, прогресс-линия, плавная смена изображения, ручное переключение и автопереход через 10 секунд. В браузере подтверждены 4 tab-контрола, ручное переключение, RU/EN-переводы и автопереход; `npx tsc --noEmit` и `git diff --check` прошли.
- По запросу пустое пространство между заголовком/описанием и анимированным меню в блоке `Мобільні застосунки` уменьшено: для этого слайдера добавлен отдельный режим `compact` с фиксированным зазором `96px`; CRM- и веб-слайдеры оставлены без изменений. В браузере подтверждены новый зазор `96px`, 4 пункта и отсутствие горизонтального overflow; `npx tsc --noEmit` и `git diff --check` прошли.

## Важно

- `public/sitegrab/assets` — это только локальные визуальные ассеты. Разметка больше не зависит от папок SiteGrab.
- Git-репозиторий рабочей папки находится на ветке `main` и опубликован в GitHub.
- Реальные формы, авторизация, база данных, серверные API и booking-интеграция пока представлены безопасными ссылками-заглушками `mailto:`.
- Git-репозиторий опубликован в `https://github.com/117Ustim/My_person_site.git`, ветка `main`, последний опубликованный коммит — `b52748a` до текущей правки.
- Старый логотип `obsidian` в шапке и футере заменён на белый знак `AU`; финальный прозрачный asset сохранён в `public/assets/au-logo-transparent.png`, чёрный фон и ореол удалены. Логотип подключён через `next/image`, desktop/mobile проверены без горизонтального overflow, `npx tsc --noEmit` и `git diff --check` проходят.

## Текущий этап — секция возможностей

- Нижняя секция `Trust is part of the product` на главной заменена на редакционный блок `CapabilitiesEssay` с заголовком `Від ідеї до продукту, яким хочеться користуватися`.
- Добавлены вступление, пять направлений (`Вебсайти`, `CRM та бізнес-системи`, `Мобільні застосунки`, `Дизайн і деталі`, `Frontend + Backend`), список технологий и CTA «Обговорити проєкт».
- Сохранён визуальный язык проекта: тёмный фон, SF Pro, крупная лёгкая типографика, коралловая нумерация, тонкие разделители и адаптивная editorial-композиция.
- Добавлен отдельный CSS Module `components/CapabilitiesEssay/CapabilitiesEssay.module.css`; desktop- и mobile-layout разделены через media queries, горизонтального overflow нет.
- Добавлены переводы нового блока для украинской, русской и английской локалей в `lib/i18n.tsx`.
- Визуально проверены desktop и mobile через браузер; консоль без ошибок и предупреждений. Сравнительный QA сохранён в `design-qa.md`, итог `final result: passed`.
- Проверки: `npx tsc --noEmit`, `git diff --check`, `npm run build` — успешно; production build сгенерировал 53 Next.js entries.
- Во время QA был обнаружен накопленный HMR-overlay `__webpack_modules__[moduleId] is not a function` после build поверх двух старых dev-процессов. Оба процесса остановлены, `.next` очищен, запущен один свежий dev-сервер на `3000`; новая вкладка загрузилась без overlay, ошибок и предупреждений.
- Заголовок `Від ідеї до продукту, яким хочеться користуватися` уменьшен ровно в два раза на desktop и mobile через CSS Module; `npx tsc --noEmit` и `git diff --check` проходят.
- Заголовки `Що я створюю`, `Вибрані проєкти` та `Від ідеї до продукту, яким хочеться користуватися` выровнены по единому размеру `clamp(38px, 4.3vw, 58px)` на desktop и mobile; отдельное уменьшение заголовка возможностей удалено.
- Общий размер этих трёх заголовков дополнительно уменьшен примерно на 10% до `clamp(34px, 3.8vw, 52px)` в `SectionHeading` и `CapabilitiesEssay`; TypeScript и `git diff --check` проходят.
- Вводный абзац, начинающийся с `Мені завжди було мало просто`, уменьшен до `clamp(18px, 1.7vw, 24px)` на desktop и `18px` на mobile; остальные описания не изменялись.
- Вводный абзац дополнительно уменьшен до `clamp(16px, 1.35vw, 19px)` на desktop и `16px` на mobile; TypeScript и `git diff --check` проходят.
- Заголовки пяти направлений в `CapabilitiesEssay` уменьшены до `clamp(24px, 2.35vw, 36px)`; описания и структура блока не изменялись.
- Удалён финальный абзац про стек из блока `Frontend + Backend`; вместо него добавлено отдельное заключение с меткой `ВИСНОВОК`, двумя новыми абзацами и отдельным визуальным разделителем. Для нового текста добавлены RU/EN-переводы.
- Первый абзац заключения уменьшен до `clamp(16px, 1.35vw, 19px)` на desktop и `16px` на mobile; второй абзац и структура заключения не изменялись.
- В `CapabilitiesEssay` добавлен небольшой круглый портрет автора справа от вступительного текста: новый asset `public/assets/founder-avatar.png`, графитовый crop, тонкая коралловая внутренняя кромка и мягкое внешнее кольцо. На mobile портрет уменьшается до `96px` и выравнивается вправо. Проверены desktop/mobile, portrait asset загружается, overflow отсутствует, console errors/warnings отсутствуют; `npx tsc --noEmit` и `git diff --check` проходят.
- Портрет перенесён из строки вступления в верхний heading-блок и теперь стоит справа напротив заголовка `Від ідеї до продукту, яким хочеться користуватися`; desktop-позиция проверена визуально, mobile-правило сохранено.
- Рамки технологических тегов обновлены в стиле проекта: добавлены мягкий графитовый фон, аккуратное скругление, светлая верхняя кромка и лёгкая внутренняя глубина; порядок технологий и адаптивная раскладка сохранены. `npx tsc --noEmit` и `git diff --check` проходят.
- Ссылка `Обговорити проєкт` в футере `CapabilitiesEssay` оформлена как CTA-кнопка в стиле меню: pill-форма, графитовый фон, коралловая обводка, стрелка и hover-подъём. `npx tsc --noEmit` и `git diff --check` проходят.
- Навигация личного портфолио переведена на четыре пункта: `Головна`, `Портфоліо`, `Про мене` и `Контакти`. Добавлен маршрут `/portfolio` с карточками выбранных работ; `/about` заменён на персональную страницу с кратким описанием подхода и навыков. Контакты ведут к футеру `#contacts`, где добавлены email и LinkedIn.
- Добавлен общий `ProjectInquiryProvider` и модальное окно заявки: CTA в шапке, мобильном меню, `CapabilitiesEssay` и `BookingCta` открывают одну форму с именем, email и описанием проекта. Отправка пока формирует письмо на `support@obsidianos.com` через `mailto:` и открывает почтовый клиент; серверная отправка через Resend/SMTP ещё не подключена.
- Визуально проверены `/`, `/portfolio`, `/about`, якорь `/#contacts` и мобильный viewport `390px`: навигация, футер и форма работают, горизонтального overflow нет, console errors отсутствуют. `npx tsc --noEmit` и `git diff --check` проходят.
- Добавлена полноценная светлая тема с переключателем рядом с языками: тёплый ivory-фон `#f3f0e9`, графитовый текст, молочно-прозрачные поверхности, terracotta-акцент `#b96158`, мягкие границы и стеклянные слои с blur. Темная тема сохранена как режим по умолчанию.
- Светлая тема протянута через общие CSS-токены в `app/layout.module.css` и основные страницы/компоненты: header, hero, карточки, FeatureSlider, CapabilityShowcase, футер, cookie banner, modal и error/loading-состояния. Логотип получает тёмную обработку в light mode, CTA используют контрастный terracotta.
- Добавлен `ThemeProvider` с сохранением выбора в localStorage и `ThemeToggle` для desktop/mobile header. Проверены переключение dark/light, сохранение темы после перезагрузки, светлая форма заявки, стеклянная шапка при прокрутке и mobile viewport `390px`; горизонтального overflow нет.
- `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли успешно; production build сгенерировал 54 статические страницы/маршрута.
- Светлая тема полностью удалена: убраны `ThemeProvider`, `ThemeToggle`, ключи локализации, ветка CSS-токенов и light-mode-обработки логотипов. Остаётся только исходная тёмная тема; переключатель удалён из desktop- и mobile-навигации. `npx tsc --noEmit`, `git diff --check` и `npm run build` проходят; сборка сгенерировала 54 статические страницы/маршрута.
- Зависший Next dev-процесс на `3000` перезапущен. Новый единственный сервер слушает `127.0.0.1:3000`; открытие главной страницы проверено в браузере: URL `http://localhost:3000/`, title `Головна`, основные заголовки загрузились.
- В футере оставлен первым оригинальный GDPR-бейдж. Остальные старые сертификационные значки заменены на пять обработанных прозрачных PNG: Full-stack development, Secure API, Responsive design, iOS & Android development и Production deployment. Новые ассеты находятся в `public/assets/badges`; визуально проверены desktop и mobile `390px`, горизонтального overflow нет. `npx tsc --noEmit`, `git diff --check` и `npm run build` проходят; сборка сгенерировала 54 статические страницы/маршрута.
- Из-за прозрачного поля новых PNG их CSS-размер увеличен с `60px` до `72px`, поэтому видимый круг каждого нового бейджа совпадает с первым GDPR-значком. Тёмная тема после перезапуска dev-сервера визуально проверена в браузере; `npx tsc --noEmit` и `git diff --check` проходят.
- Ряд из шести футер-бейджей переведён в режим без переноса: desktop-gap уменьшен до `10px`, поэтому все значки стоят в одну строку. На mobile заданы пропорционально уменьшенные размеры и `6px` gap, чтобы ряд сохранялся без горизонтального overflow. Desktop-визуальная проверка пройдена; `npx tsc --noEmit` и `git diff --check` проходят.
- С бейджей футера удалены все ссылки: изображения GDPR и пяти новых значков теперь обычные некликабельные `Image` без переходов. `npx tsc --noEmit` и `git diff --check` проходят.
- В верхней границе футера добавлен декоративный горизонтальный разделитель: тонкая графитовая линия с коротким коралловым акцентом и мягким свечением в общей стилистике сайта. `npx tsc --noEmit` и `git diff --check` проходят.
- Коралловый акцент у разделителя футера убран; оставлена нейтральная графитовая линия с мягким свечением. `npx tsc --noEmit` и `git diff --check` проходят.
- Из футера удалён нижний блок с юридическим дисклеймером, подписью `Працює на` и логотипом RockCore; связанные стили и неиспользуемые переводы удалены, остальная сетка футера сохранена. `npx tsc --noEmit` и `git diff --check` проходят.
- Футер переведён на персональные контакты: `ustik72@gmail.com`, `+380 67 327 6040`, Telegram через `https://t.me/+380673276040` и Viber через `viber://chat?number=%2B380673276040`. Email и телефон кликабельны, Telegram и Viber оформлены нейтральными SVG-иконками-кнопками без кораллового hover-акцента.
- Старые данные Obsidian в описании футера и колонке навигации заменены на описание независимого full-stack-разработчика из Киева; добавлены переводы RU/EN/UA и нейтральная строка copyright. Все старые `support@obsidianos.com` в CTA, форме заявки и страницах интеграций заменены на `ustik72@gmail.com`.
- Проверки после обновления контактов: `npx tsc --noEmit`, `git diff --check`, `npm run build` — успешно; production build сгенерировал 54 статические страницы/маршрута.
- Три устаревшие колонки ссылок в правой части футера заменены на отдельный интерактивный компонент `FooterProcess`: `Ідея → Структура → Дизайн → Розробка → Запуск`. На desktop этапы расположены горизонтально, на mobile — вертикально; активный этап автоматически меняется каждые 2,8 секунды, при hover/focus/click показывается краткое описание, а движение приостанавливается во время взаимодействия.
- Новая схема использует только нейтральные графитовые и бело-серые оттенки без кораллового акцента. Добавлены тонкая линия, круглые узлы, проходящий по всей траектории световой импульс, стеклянная поверхность и обработка `prefers-reduced-motion`; тексты переведены для UA/RU/EN.
- Визуальная проверка сохранена в `design-qa.md`: desktop `/tmp/footer-process-desktop-final.png`, mobile `/tmp/footer-process-mobile-final.png`, общий comparison `/tmp/footer-process-comparison-final.png`, итог `final result: passed`. Проверены пять интерактивных шагов, отсутствие mobile-overflow и чистая консоль.
- После `npm run build` обнаружен конфликт двух старых dev-процессов за `.next`; остановлены только процессы текущего проекта, очищен генерируемый кэш и запущен один свежий сервер на `127.0.0.1:3000`. Финальная вкладка открывается без runtime-ошибок.
- Проверки новой схемы: `npx tsc --noEmit`, `git diff --check`, `npm run build` — успешно; production build сгенерировал 54 статические страницы/маршрута.
- Из футера удалён отображаемый номер телефона по запросу; email, Telegram и Viber сохранены без изменений. `footer.phone` оставлен в словаре переводов для безопасной совместимости, но в разметке футера больше не используется.
- Текст описания футера заменён на утверждённый вариант: `Приватний розробник. Перетворюю складні ідеї на зрозумілі сайти, CRM-системи та мобільні застосунки.`. В блок контактов добавлена отдельная строка местоположения `Україна, Київ` с переводами RU/EN; email и социальные ссылки сохранены.
- После обновления описания и местоположения повторно пройдены `npx tsc --noEmit`, `git diff --check` и `npm run build`; production build сгенерировал 54 статические страницы/маршрута.
- Подпись местоположения в контактах заменена с `Місто` на `Локація`; для RU/EN установлены `Локация` и `Location`.
- Большой декоративный watermark `obsidian` полностью удалён из футера вместе со связанными стилями; логотип AU, контакты, бейджи и схема процесса сохранены.
- После запуска сборки поверх активного dev-сервера повредился сгенерированный `.next`-кеш: HTML открывался без применённых стилей, поэтому фон и шрифт выглядели как стандартные браузерные. Кеш `.next` удалён, оставлен один свежий Next.js dev-сервер на `127.0.0.1:3000`; ответ страницы и CSS проверены с HTTP 200, тёмные токены фона снова присутствуют.
- Нижняя строка футера вынесена на всю ширину: copyright остался слева, а справа добавлены подпись `Розроблено` и компактный логотип AU studio, выровненный по высоте строки. Добавлены переводы подписи для RU/EN; проверены `npx tsc --noEmit`, `git diff --check`, HTTP 200 страницы и наличие новых CSS-классов в runtime.
- Страница `/portfolio` полностью переведена со статического hero и 2×2 сетки на `PortfolioCarousel` в композиции референса: активный проект как полноэкранный фон, copy слева, три интерактивных вертикальных превью справа и нейтральные круглые стрелки. Используются реальные локальные ассеты проектов и `next/image`; по клику, стрелкам и клавишам `←/→` проект переключается, CTA ведёт к `#contacts`. Добавлены UA/RU/EN-подписи, reduced-motion и mobile scroll-snap лента. Установлен `lucide-react` для локальных доступных стрелок; `npx tsc --noEmit`, `git diff --check`, runtime и desktop interaction checks прошли.
- В верхнюю часть `/portfolio` возвращён компактный `MarketingHero` с исходными надписью `Продукти, які вирішують реальні задачі`, описанием и атмосферным тёмным фоном. Сразу под hero оставлен новый `PortfolioCarousel`, старая 2×2 сетка не возвращалась. Проверены порядок блоков, переключение проекта, отсутствие горизонтального overflow, TypeScript и `git diff --check`.
- В большой активной карточке `PortfolioCarousel` добавлен выбранный эффект glass aperture: отдельный RGBA-ассет `public/assets/transitions/glass-aperture-veil.png` движется в сторону нажатой стрелки, под ним прежний фон скрывается через clip-path и открывает следующий проект. На время 820 ms стрелки и preview-кнопки блокируются от повторного клика; новый текст появляется после прохождения завесы. Обратное направление, отсутствие overflow, runtime без ошибок, `npx tsc --noEmit` и `git diff --check` проверены. Для `prefers-reduced-motion` переключение остаётся мгновенным.
- Переход `PortfolioCarousel` сделан плавнее: длительность синхронно увеличена с `820ms` до `960ms`, easing движения завесы и старого фона смягчён, а появление нового текста стало менее резким за счёт уменьшения blur. `npx tsc --noEmit` и `git diff --check` проходят.
- Исправлена runtime-ошибка `Cannot find module './vendor-chunks/next.js'`: остановлен только старый dev-процесс этого проекта, повреждённый сгенерированный `.next` перемещён в `/private/tmp/person-site-next-cache-20260916-1315`, затем запущен один свежий сервер на `http://localhost:3000`. Маршрут `/portfolio` отдаёт HTTP 200 без текста ошибки, на порту 3000 остался один процесс; `npx tsc --noEmit` и `git diff --check` проходят.
- В `/portfolio` заменены четыре изображения карусели на новые локальные проекты: Beauty Master CRM, VetScanCT для ветеринарной клиники, Luxury Travel и спортивная CRM. Файлы добавлены в `public/assets/portfolio`, для всех проектов обновлены названия, описания и alt-тексты на UA/RU/EN; слайдер и его glass-aperture-анимация сохранены. Все четыре изображения отдаются с HTTP 200, `/portfolio` отвечает HTTP 200, `npx tsc --noEmit` и `git diff --check` проходят.
- Активное изображение в `PortfolioCarousel` переведено из полноэкранного crop в отдельную адаптивную glass-рамку с `object-fit: contain`, двойной графитовой кромкой, мягкой тенью и размытым фоновым слоем. Старое изображение во время перехода теперь также раскрывается внутри рамки; desktop/mobile-правила сохранены, горизонтального overflow и console errors нет. `npx tsc --noEmit`, `git diff --check` и `/portfolio` HTTP 200 проходят.
- По запросу удалён размытый фон вокруг glass-карточки: убраны backdrop-изображение и внешний overlay, оставлен чистый тёмный фон проекта. Рамка, полное отображение изображения, превью и переключение сохранены; визуально проверено в Chrome, backdrop/overlay в DOM отсутствуют, console errors нет, overflow отсутствует. `npx tsc --noEmit` и `git diff --check` проходят.
- `PortfolioCarousel` переразложен на три зоны внутри статичной внешней glass-оболочки: слева текст, по центру отдельная `mainMediaFrame` с полным изображением, справа вертикальные preview-карточки. `glass-aperture` и outgoing wipe перенесены внутрь центральной media-карточки, `overflow: hidden` ограничивает эффект её границами; внешняя оболочка и текст не анимируются. Desktop визуально проверен: зоны не перекрываются, `object-fit: contain`, три строки превью, overflow отсутствует, console errors нет; `npx tsc --noEmit` и `git diff --check` проходят.
- Исправлено предупреждение React о дублирующихся ключах в анимации `PortfolioCarousel`: outgoing-слой и glass-aperture теперь используют уникальные ключи с суффиксами `-outgoing` и `-aperture`. `npx tsc --noEmit` и `git diff --check` проходят.
- Для VetScanCT в центральную media-рамку подключён длинный оригинал `vetscanct-crm-portfolio.jpg`; отдельное превью справа продолжает использовать компактный `vetscanct-crm.png`. Для scrollable-проекта добавлены внутренняя вертикальная прокрутка без crop, доступ с клавиатуры, нейтральный scrollbar, локализованная подсказка с анимированной стрелкой и индикатор прогресса; подсказка скрывается у конца изображения, позиция сбрасывается при смене проекта. Chrome-проверка показала `scrollHeight` 2431 при высоте рамки 566, корректный scrollTop до 1864.5, сброс к 0 после возврата и отсутствие console errors/warnings; `npx tsc --noEmit` и `git diff --check` проходят.
- Левая зона `PortfolioCarousel` сделана компактнее: ширина текстовой колонки уменьшена примерно с 366 до 275 px, шрифты и отступы снижены; освободившееся пространство передано центральной media-рамке, её ширина увеличена примерно с 804 до 897 px. Правая вертикальная колонка не менялась, overflow отсутствует; desktop-визуальная проверка, `npx tsc --noEmit` и `git diff --check` проходят.
- Для заголовка VetScanCT добавлен отдельный компактный размер шрифта (`clamp(26px, 2.15vw, 38px)`), остальные проекты не изменены. `npx tsc --noEmit` и `git diff --check` проходят.
- Компактный размер заголовка (`clamp(26px, 2.15vw, 38px)`) перенесён на все проекты `PortfolioCarousel`, чтобы при переключении карточек заголовки имели единую типографику. Визуальная проверка трёх проектов показала одинаковый computed-размер около 36.8 px; console errors/warnings нет, `npx tsc --noEmit` и `git diff --check` проходят.
- В `PortfolioCarousel` добавлены три одинаковые glass-кнопки-фильтра над центральным фото: «Сайти», «CRM-системи», «Мобільні застосунки». По умолчанию активна категория `sites`, поэтому при загрузке показывается только Luxury Travel; Beauty Master CRM и VetScanCT отнесены к CRM, спортивная CRM — к мобильным приложениям. Переключение обновляет центральный проект, счётчик и правые превью; UA/RU/EN подписи добавлены в i18n. Chrome-проверка всех категорий прошла без console errors/warnings и горизонтального overflow; `npx tsc --noEmit` и `git diff --check` проходят.
- Заголовок верхнего hero-блока страницы `/portfolio` уменьшен примерно в два раза через отдельный класс `portfolioHero`: desktop-размер ограничен 52 px, добавлена адаптивная мобильная версия. Остальные компактные hero-блоки сайта не затронуты; Chrome-визуальная проверка, `npx tsc --noEmit` и `git diff --check` проходят.
- Только для `/portfolio` создан отдельный `PortfolioCta`; общий `BookingCta` и CTA на остальных страницах не менялись. Новая карточка показывает композицию «эскиз → wireframe → интерфейс» из трёх анимированных графитово-серых стеклянных слоёв, реагирует мягким светом на курсор и собирает слои при нажатии кнопки перед открытием формы. Добавлены отдельные UA/RU/EN-тексты CTA; при `prefers-reduced-motion` движение и задержка отключаются.
- Проверки `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли. Повреждённый кэш старого dev-сервера был очищен, сервер проекта перезапущен; `/portfolio` возвращает HTTP 200 и содержит новый CTA.
- Только правая визуальная часть `PortfolioCta` на `/portfolio` заменена на «живую» колоду из реальных проектов Luxury Travel, VetScanCT и Beauty Master CRM. Карточки содержат фото, название, категорию, стек, статус и счётчик; активный проект автоматически меняется раз в 6,5 секунды, при hover/focus ротация ставится на паузу, карточки раскрываются веером, реагируют на курсор, получают серебристый блик и плавно перестраивают глубину. При нажатии CTA колода собирается перед открытием формы; для `prefers-reduced-motion` движение отключается.
- Во время browser-QA исправлен stacking context анимированных оболочек: front/middle/back z-index теперь синхронизирован с активным проектом, поэтому выбранная карточка действительно выходит вперёд. Проверены ручной выбор видимой карточки, desktop, mobile 390 × 844, отсутствие горизонтального overflow и чистая консоль. Сравнение с выбранным концептом сохранено в `/tmp/portfolio-cta-live-deck-comparison.png`, итоговый раздел добавлен в `design-qa.md` с `final result: passed`.
- Финальные проверки новой колоды: `npx tsc --noEmit`, `git diff --check`, `npm run build` — успешно; production build сгенерировал 54 статические страницы/маршрута.
- В `PortfolioCta` исправлен показ изображений: `object-fit: cover` заменён на `contain`, масштабирование и сдвиг изображения убраны, чтобы верхний и боковые края всех фото оставались видимыми. «Живость» карточек сохранена через мягкую анимацию яркости/насыщенности; высота visual/deck увеличена для уменьшения letterboxing. Desktop и mobile проверены, mobile overflow `375 = 375`, чистая вкладка после перезапуска dev-сервера без console errors/warnings.
- По результатам design-QA сохранены `/tmp/portfolio-cta-full-images-desktop-final.png`, `/tmp/portfolio-cta-clean-mobile.png` и `/tmp/portfolio-cta-full-images-comparison.png`; новый раздел в `design-qa.md` завершён статусом `final result: passed`. Повторные `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли; production build сгенерировал 54 маршрута.
- Интервал автоматической смены карточек в `PortfolioCta` уменьшен с 6,5 до 3 секунд. Пауза при наведении и фокусе сохранена.
- Интервал автоматической смены карточек в `PortfolioCta` изменён с 3 до 4 секунд. Пауза при наведении и фокусе сохранена.
- В `PortfolioCta` радиус верхних углов изображения синхронизирован с радиусом всей проектной карточки через общий CSS-токен; desktop и mobile сохраняют свои размеры скругления.
- Устранено визуальное расхождение радиуса: изображения в `PortfolioCta` теперь рендерятся в собственных пропорциональных рамках с `max-width/max-height` и получают тот же радиус, что карточка; `object-fit: contain` больше не оставляет прямые углы у видимой области фото.
- В `PortfolioCta` выровнены отступы изображения: одинаковый inset сверху и по бокам, верхнее скругление сохранено, нижние углы изображения сделаны прямыми; размер фото адаптируется по высоте без обрезки.
- Для VetScanCT в `PortfolioCta` добавлен увеличенный режим изображения с меньшим внутренним inset; все фото выровнены к верхнему краю, а видимая картинка дополнительно clipped с радиусом только сверху.
- Усилена визуальная правка `PortfolioCta`: стандартный inset уменьшен до 20px, VetScanCT — до 5px, для верхних углов добавлена отдельная внутренняя окантовка; нижние углы остаются прямыми.
- По утверждённому референсу удалены лишняя внутренняя обводка и отдельное масштабирование VetScanCT в `PortfolioCta`: все карточки используют одинаковые чистые отступы фото, скругление только сверху и прямой стык с текстовой нижней частью.
- Исправлен повреждённый `.next`-кэш: остановлен зависший dev-процесс, удалён только сгенерированный кэш проекта, запущен один свежий Next.js-сервер на `3000`; HTML и все CSS-файлы `/` снова возвращают `200`.
- Финальная сверка `PortfolioCta` с референсом от 2026-09-17 00:41: удалены дополнительная внутренняя обводка и индивидуальное масштабирование VetScanCT. У всех трёх карточек одинаковые отступы изображения (20px desktop / 12px mobile), скруглены только верхние углы изображения, нижние углы прямые, а текстовый footer остаётся единым с внешней карточкой. Browser-QA подтвердил отсутствие внутренней рамки и корректный ручной выбор карточки.
- Уточнена последняя визуальная правка по референсу: отдельный тёмный фон области изображения заменён на прозрачный, а основной фон карточки сделан графитовым. Теперь inset вокруг фото является частью единой карточки, а не выглядит как нарисованная внутренняя рамка.
- По уточнению пользователя все изображения в `PortfolioCta` переведены на единый режим заполнения области (`object-fit: cover`, `width/height: 100%`): фото крупно занимают весь верх карточки, имеют одинаковый inset и скругление только сверху — как Beauty Master CRM на референсе, без пустых тёмных полей.
- Для одной карточки VetScanCT добавлено адресное позиционирование изображения `object-position: left center`, чтобы полностью показать левую часть макета. Luxury Travel и Beauty Master CRM не изменялись.
- В CTA-кнопке `PortfolioCta` исправлено вертикальное выравнивание: текст и стрелка теперь центрируются внутри кнопки через `align-items: center`.
- В `FooterProcess` добавлен мягкий импульс активного этапа: внешний кружок и внутреннее ядро плавно увеличиваются, усиливают свечение и возвращаются к исходному размеру; при `prefers-reduced-motion` анимация отключается.
- Анимации `FooterProcess` синхронизированы: бегущий импульс замедлен до 20 секунд, делает остановку на последней точке, а активный кружок переключается в момент касания импульсом соответствующего узла. Длительность вспышки кружочка увеличена до 1500 мс; при наведении/фокусе линия и этапы ставятся на паузу вместе.
- Момент активации узла скорректирован на 320 мс раньше центра: вспышка начинается, когда передний край бегущего импульса касается левой стороны кружочка, а не когда импульс уже проходит его центр.
- Рассинхронизация устранена: движение импульса и выбор активного этапа теперь рассчитываются одним `requestAnimationFrame`-прогрессом. Пауза при наведении/фокусе сохраняет одну и ту же позицию линии и этапа, поэтому вспышка запускается именно при контакте с левой стороной узла.
- Исправлена направленность упреждения активации: теперь к общему прогрессу добавляется 320 мс, поэтому узел включается перед центром бегунка, а не на несколько секунд раньше.
- Бегущий импульс в `FooterProcess` сделан немного крупнее: размер увеличен до 8 × 8 px, свечение усилено пропорционально; скорость, синхронизация и логика вспышки кружочков не менялись.
- По уточнению пользователя размер бегущего импульса в `FooterProcess` увеличен до 15 × 15 px; остальная анимация не изменялась.
- Ширина бегущего импульса в `FooterProcess` увеличена вдвое — до 30 px при высоте 15 px; форма стала мягкой капсулой, логика движения и синхронизации не менялась.
- Ширина бегущего импульса в `FooterProcess` скорректирована до 25 px при высоте 15 px; форма капсулы и логика анимации сохранены.
- В раздел «CRM-системи» на странице `/portfolio` добавлено длинное изображение `beauty-master-crm-portfolio.jpg` для Beauty Master CRM. Карточка использует внутреннюю вертикальную прокрутку и реальные пропорции изображения `1600 × 7240`; VetScanCT сохранён без изменений.
- Для Beauty Master CRM в правой ленте CRM-карточек подключён отдельный preview `beauty-master-crm.png` (первый скрин), а по нажатию открывается длинный `beauty-master-crm-portfolio.jpg` (второй скрин) в центральной прокручиваемой рамке.
- По запросу пользователя переходы `PortfolioCarousel` переведены на мгновенную замену: для сайтов, CRM-систем и мобильных приложений удалены outgoing/aperture-слои, чтобы старая картинка не оставалась поверх новой и изображение сразу открывалось в полном масштабе. Прокрутка активного длинного изображения по-прежнему сбрасывается наверх.
- Спортивная CRM добавлена в раздел «CRM-системи»: справа используется preview `sports-crm.png`, а после выбора открывается длинный вертикальный файл с внутренней прокруткой. Прежняя компактная версия проекта сохранена в «Мобільні застосунки», чтобы раздел не оставался пустым.
- Центральное изображение спортивной CRM заменено на `sport-base-crm-vertical-portfolio.png` (`2048 × 17900`); preview справа, карточка и остальные проекты не изменялись.
- Для ускорения загрузки спортивной CRM подключена оптимизированная JPEG-копия `sport-base-crm-vertical-portfolio-1600.jpg` (`1600 × 13984`, около 2,3 МБ вместо 9,1 МБ PNG); визуальный preview справа сохранён.
- Проверены все три центральных CRM-изображения в браузере: Beauty Master CRM (`1,8 МБ`), VetScanCT (`1,3 МБ`) и спортивная CRM (`2,3 МБ` после оптимизации). Все остаются резкими, прокручиваемыми и без ошибок; `npm run build` завершился успешно.
- Исправлена runtime-ошибка Webpack `__webpack_modules__[moduleId] is not a function`: остановлен старый dev-процесс, удалён только сгенерированный `.next`-кэш, затем запущен один чистый Next.js dev-сервер на `3000`. Свежая страница `/portfolio` открывается, консоль без errors/warnings.
- В `PortfolioCarousel` правая лента теперь показывает все карточки выбранной категории, включая активную карточку из центрального окна. Активная карточка получает мягкое выделение и остаётся доступной для выбора.
- В «CRM-системи» добавлен Auto Service CRM: preview `auto-service-crm-promo-uk.png` показывается в правой ленте, а по нажатию открывается прокручиваемый `ukr-auto-service-crm-vertical-portfolio.png` (`1600 × 7550`). Для четырёх CRM-карточек правая лента переведена на автоматические grid-строки.
- Проверка Auto Service CRM в браузере: 4 карточки видны справа, preview совпадает с первым скрином, центральное изображение совпадает со вторым и прокручивается; переходных overlay-слоёв и console errors/warnings нет. `npx tsc --noEmit` и `git diff --check` прошли.
- Превью в `PortfolioCarousel` нормализованы под фактическую desktop-форму правой карточки `2:1`: для пяти проектов добавлены облегчённые JPEG-кадры `1200×600` с полностью сохранённым исходником и мягким продолжением фона по бокам. Для mobile добавлены квадратные `800×800` версии, чтобы изображения не обрезались в горизонтальной ленте. В центральных длинных изображениях и их сценариях ничего не менялось.
- В `FeatureItem` добавлен `previewImageMobile`; `PortfolioCarousel` переключает desktop/mobile-превью через CSS media-query. Desktop CRM-лента проверена в браузере: все четыре карточки одинаковые `258×129`, фото заполняют всю область без пустых зон, console errors/warnings нет. `npx tsc --noEmit` и `git diff --check` проходят.
- Правая колонка `PortfolioCarousel` сделана компактнее только на desktop/tablet: третья grid-колонка уменьшена с `0.58fr` до `0.45fr` (на текущем desktop карточки стали `208px` вместо `258px`), центральному окну передано освободившееся место. Размеры и режим отображения фотографий не изменялись; browser-QA подтвердил четыре одинаковые карточки и чистую консоль. `npx tsc --noEmit` и `git diff --check` проходят.
- Для проекта Luxury Travel в категории «Сайти» сохранено прежнее горизонтальное превью `/assets/portfolio/luxury-travel.png`, а центральное изображение заменено на предоставленный вертикальный коллаж из шести экранов. Коллаж сохранён без обрезки как `1528×6798` JPEG (`2,4 МБ` вместо исходного PNG `12 МБ`), подключён к прокручиваемой центральной области с возвратом к началу при выборе карточки. Browser-QA подтвердил правильные источники, прокрутку, сохранение CRM-категории и отсутствие console errors/warnings; `npx tsc --noEmit` и `git diff --check` проходят.
- Одиночная карточка Luxury Travel в правой колонке больше не растягивается на всю высоту: получила ту же высоту `129px`, что и CRM-карточки (`208×129` на текущем desktop). Для неё сохранён `object-fit: contain`, поэтому исходное превью не подрезается; CRM-лента по-прежнему использует `cover`. Browser-QA подтвердил правильный центральный вертикальный скрин, прокрутку и чистую консоль; `npx tsc --noEmit` и `git diff --check` проходят.
- В превью-карточках добавлен отдельный мягкий backdrop-слой: если исходник не заполняет всю область, свободное место заполняется размытым продолжением того же изображения вместо тёмных полей. Для CRM, где превью уже нормализованы и пустых зон нет, фоновой дубликат не рендерится — лишние запросы не создаются. Проверены Luxury Travel и CRM-лента, источники центральных изображений не изменились, console errors/warnings нет; `npx tsc --noEmit` и `git diff --check` проходят.
- В категорию «Сайти» добавлен отдельный проект VetScanCT: справа используется нормализованное превью из предоставленного промо-скрина, а по нажатию открывается вертикальный `vetscanct-site-vertical-portfolio.jpg` (`2112×7010`) с прокруткой. Существующий VetScanCT в CRM оставлен отдельно. Для двух карточек сайтов зафиксированы одинаковые строки `208×129`, Luxury Travel переведён на нормализованное превью; browser-QA подтвердил правильные источники, прокрутку и чистую консоль. `npx tsc --noEmit` и `git diff --check` проходят.
- Превью VetScanCT в разделе «Сайти» заменено на новый украинский файл `vetscanct-site-preview-card-ua.jpg` и его mobile-версию; центральный `vetscanct-site-vertical-portfolio.jpg`, CRM-версия и остальные карточки не менялись. Использовано новое имя ассета, чтобы исключить выдачу старой картинки из кэша. Browser-QA подтвердил новый источник превью, прокручиваемый центральный коллаж и чистую консоль; `npx tsc --noEmit` и `git diff --check` проходят.
- В раздел «Сайти» добавлена третья карточка «Дитячі свята — яскравий сайт»: preview `childrens-party-preview-card.jpg` показывает первый предоставленный скрин, а по нажатию открывается `childrens-party-vertical-portfolio.jpg` (`1496×5194`) с вертикальной прокруткой. Превью оптимизировано до desktop `1200×600` и mobile `800×800`, центральный PNG сохранён как JPEG `1,5 МБ`. Все три site-карточки имеют одинаковый размер `208×129`, CRM осталась отдельной категорией; browser-QA и проверки TypeScript/форматирования прошли без ошибок.
- В раздел «Сайти» добавлена карточка «Оливкова олія — преміумсайт»: preview `olive-oil-preview-card.jpg` показывает первый предоставленный скрин, а по нажатию открывается `olive-oil-vertical-portfolio.jpg` (`1496×4490`) с вертикальной прокруткой. Превью оптимизировано до desktop `1200×600` и mobile `800×800` с мягким продолжением фона без обрезки исходника; все четыре site-карточки имеют одинаковый размер `208×129`, CRM-категория не изменялась. Browser-QA подтвердил правильные источники, прокрутку и сохранение CRM-карточек; `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли без ошибок.
- В раздел «Сайти» добавлена карточка «Модульні будинки — сайт»: preview `modular-house-preview-card.jpg` показывает первый предоставленный скрин, а по нажатию открывается `modular-house-vertical-portfolio.jpg` (`1604×5540`) с вертикальной прокруткой. Превью оптимизировано до desktop `1200×600` и mobile `800×800` с мягким продолжением лесного фона без обрезки исходника; все пять site-карточек имеют одинаковый размер `208×129`, CRM-категория не изменялась. Browser-QA подтвердил правильные источники, прокрутку и сохранение CRM-карточек; `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли без ошибок.
- В `PortfolioCarousel` для категорий с более чем четырьмя проектами добавлена вертикальная прокрутка правой ленты: появляется аккуратный пульсирующий индикатор со стрелкой вниз, который плавно скрывается в конце списка. При смене категории позиция ленты сбрасывается наверх; на mobile сохранена горизонтальная лента, а CRM из четырёх карточек не получает лишний скролл. Browser-QA подтвердил прокрутку, выбор пятой карточки, сброс позиции, отсутствие индикатора у CRM и отсутствие runtime overlay; `npx tsc --noEmit` и `git diff --check` прошли.
- Добавлен глобальный `FloatingActions` в `SiteShell`: в правом нижнем углу отображается кнопка сообщения, открывающая существующее модальное окно заявки, и кнопка возврата наверх, появляющаяся после прокрутки более чем на 240 px. Использованы доступные иконки `ArrowUp` и `MessageCircle`, локализованные aria-label для UA/RU/EN, адаптивные размеры, hover/focus-состояния и `prefers-reduced-motion`. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли; browser-QA подтвердил открытие формы, появление стрелки после скролла и чистую консоль.
- В последнюю карточку категории `Сайти` добавлен проект `IDENTO`: первый предоставленный скрин подготовлен в desktop-превью 1200×600 без обрезки композиции, второй вертикальный коллаж подключён как центральное прокручиваемое изображение `1623×5197`. Добавлены ассеты `idento-presentation-dark.png`, `idento-presentation-dark-preview.png` и оптимизированный `idento-vertical-collage.jpg`, локализованные UA/RU/EN title/description/alt. Проверены порядок `06 / 06`, источники обоих изображений, desktop/mobile preview, `npx tsc --noEmit`, `git diff --check`, `npm run build` и browser-QA без console errors/warnings.
- Вкладки категорий `PortfolioCarousel` переработаны в интерактивный `Liquid Glass Tabs`: добавлена единая стеклянная подложка, которая плавно переезжает между вкладками, перелив света, локальный hover-блик по координатам курсора, лёгкий 3D-наклон и ripple-вспышка при нажатии. Позиция и ширина индикатора пересчитываются при смене категории, изменении языка и resize; для mobile сохранены компактные размеры, для `prefers-reduced-motion` анимации отключаются. Browser-QA desktop/mobile, переключение трёх категорий и console errors/warnings прошли; `npx tsc --noEmit`, `git diff --check` и `npm run build` успешны.
- Устранён runtime-сбой Next.js `Cannot find module './331.js'`: подтверждён рассинхрон сгенерированного `.next/server/webpack-runtime.js` и чанков после предыдущих dev/build-запусков. Повреждённый `.next` перемещён в `/private/tmp/person-site-next-cache-20260918-chunk331-error` для восстановления, свежий dev-сервер пересобрал кэш; `/portfolio` проверен с HTTP 200, ошибка chunk 331 больше не воспроизводится. Запрос `/favicon.ico` теперь возвращает обычный HTTP 404, без ошибки сборки.
- В верхнюю строку карточки `PortfolioCarousel` добавлена стеклянная кнопка перехода на рабочий сайт рядом со счётчиком проекта. Пока URL не задан, отображается доступная заглушка с локализованной подсказкой; для будущих ссылок в `FeatureItem` подготовлено поле `liveUrl`. Внутренний отступ контента увеличен слева, на mobile добавлены адаптивные боковые поля. `npx tsc --noEmit` и `git diff --check` проходят.
- По уточнённому референсу кнопка рабочего сайта перенесена в отдельный верхний блок левой колонки над надписью «ПОРТФОЛИО»; вертикальный отступ до надписи задан ровно `30px`, счётчик проекта оставлен справа. `npx tsc --noEmit` и `git diff --check` проходят.
- Всплывающая карточка кнопки рабочего сайта перенесена сверху справа от кнопки; текст заглушки и будущей ссылки локализован как «Перейти на сайт» / `Go to website`. Для placeholder-кнопки tooltip теперь также показывает именно этот текст.
- Для устранения повторяющихся ошибок рассинхронизации `.next` разделены директории сборки: dev использует `.next-dev`, production build/start — `.next-build`; пути добавлены в `.gitignore`, версия Next.js зафиксирована на `15.5.25`. Старый повреждённый `.next` сохранён отдельно во временной папке после проверки.
- Раздельная схема проверена на практике: чистый dev-сервер отдал `/portfolio` с HTTP 200 и `favicon.ico` с обычным HTTP 404; `npm run build` завершился успешно с 54 статическими маршрутами; production `npm run start` из `.next-build` также отдал `/portfolio` с HTTP 200. Старые процессы Next.js, занимавшие порт 3000, остановлены.
- У кастомной карточки кнопки рабочего сайта удалён нативный `title`, чтобы браузер больше не показывал белую системную подсказку. Кастомный tooltip выровнен справа от кнопки через `left: calc(100% + 8px)`, поэтому его левая граница начинается после правого края кнопки и текст отображается полностью.
- Кнопка рабочего сайта усилена визуально: увеличена до `44px` (на mobile `40px`), добавлены контрастная стеклянная заливка, коралловое свечение, пульсирующий внешний контур, мягкая breathing-анимация и реакция иконки при наведении. Вся кнопка сдвинута вправо на `15px`, а `prefers-reduced-motion` отключает движение.
- Плавающие кнопки «вверх» и «сообщение» сдвинуты ещё на `10px` вправо: desktop через адаптивный `right`, mobile — до `4px` от края. Размеры, расстояние и поведение кнопок не менялись.
- Кнопка перехода на сайт над «ПОРТФОЛИО» сдвинута на `10px` влево: общий горизонтальный сдвиг изменён с `15px` до `5px` вправо от исходной позиции. Анимация и оформление сохранены.
- Кнопка перехода на сайт дополнительно сдвинута на `4px` влево: текущий сдвиг установлен на `1px` вправо от исходной позиции.
- Оптимизирована загрузка `/portfolio`: убран `priority` у центральных и CTA-изображений, добавлены lazy loading и сниженное качество для превью; вертикальные портфолио и CTA-скриншоты переведены в WebP без изменения размеров. Самые тяжёлые используемые файлы уменьшены примерно с `2,2–2,4 МБ` до `0,18–0,59 МБ`, а `sport-base-crm` — с `2,3 МБ` до `0,42 МБ`.
- Ускорено переключение проектов в центральном окне: уже оптимизированные WebP открываются без повторной серверной обработки Next Image, а полный скрин предварительно подгружается при наведении или фокусе на preview-карточке. Повторный клик использует браузерный кэш вместо ожидания новой оптимизации.
- Для CTA-кнопки `PortfolioCta` добавлен эффект «парения»: подъём на `7px`, лёгкое масштабирование, глубокая контактная тень, мягкое коралловое свечение и однократный световой проход по поверхности. При `prefers-reduced-motion` остаётся статичное состояние.
- В первой карточке блока «Що я створюю» на главной (`Вебсайти та вебзастосунки`) изображение заменено на точный asset Luxury Travel `/assets/portfolio/luxury-travel.png`; alt-текст локализован для UA/RU/EN. Нижний слайдер «Вебсайти та лендинги» оставлен без изменений. Production-сборка, `npx tsc --noEmit`, `git diff --check` и browser-QA прошли; источник изображения подтверждён, console errors/warnings отсутствуют.
- Для первой карточки Luxury Travel задано отдельное позиционирование изображения `object-position: center top`: верхняя часть исходного скрина теперь видна полностью, а режимы остальных карточек не изменялись. `npm run build`, `npx tsc --noEmit`, `git diff --check` и browser-QA прошли; console errors/warnings отсутствуют.
- Во второй карточке блока «Що я створюю» на главной (`CRM-системи та внутрішні кабінети`) изображение заменено на точный asset Beauty Master CRM `/assets/portfolio/beauty-master-crm.png`; alt-текст локализован для UA/RU/EN. Production-сборка, TypeScript, `git diff --check` и browser-QA прошли, console errors/warnings отсутствуют.
- В карточку «Серверна частина та інфраструктура» добавлена технология `Docker` после GitHub. `npx tsc --noEmit` и `git diff --check` прошли.
- В ту же карточку «Серверна частина та інфраструктура» подключён присланный скрин Docker Desktop как точный asset `/assets/portfolio/docker-desktop.png` (`2048×1285`); изображение закреплено сверху, чтобы синяя панель Docker была видна. Alt-текст локализован для UA/RU/EN. `npm run build`, `npx tsc --noEmit`, `git diff --check` и browser-QA прошли, console errors/warnings отсутствуют.
- Скрин Docker Desktop в backend-карточке слегка затемнён через локальный класс `imageDimmed` (`filter: brightness(0.9)`); остальные изображения и текст не изменялись. `npm run build`, `npx tsc --noEmit`, `git diff --check` и browser-QA прошли, console errors/warnings отсутствуют.
- У изображений в блоке «Що я створюю» добавлены отдельные уровни затемнения: сайты — 4% (`brightness(0.96)`), CRM — 6% (`brightness(0.94)`), Docker сохранён на 10% (`brightness(0.9)`). Затемнение применяется только к трём указанным скринам; `npm run build`, `npx tsc --noEmit` и `git diff --check` прошли.
- В третью карточку блока «Що я створюю» на главной (`Мобільні застосунки`) подключён точный asset фитнес-приложения `/assets/portfolio/fitness-mobile-app.png` (`2400×1500`), локализован alt-текст и задано верхнее позиционирование, чтобы заголовок скрина не обрезался. `npm run build`, `npx tsc --noEmit`, `git diff --check` и browser-QA прошли, console errors/warnings отсутствуют.
- В первом слайдере блока «CRM-системи та автоматизація» первый пункт заменён на «CRM-система ветеринарної клініки» с точным asset VetScanCT `/assets/portfolio/vetscanct-home-crm.png` (`1671×941`). Добавлены локализованные title/description/alt, первый пункт активен по умолчанию и возвращается при выборе; для полного показа скрина включён `imageFit: 'contain'`. `npm run build`, `npx tsc --noEmit`, `git diff --check` и browser-QA прошли, console errors/warnings отсутствуют.
- Визуальная карточка первого CRM-слайда переведена на пропорции исходного VetScanCT-скрина `1671:941`: фиксированная высота убрана только для режима `imageFit: 'contain'`, поэтому фото отображается без больших пустых полос сверху и снизу. Проверено в production preview: визуальная область `872×491.05px`, `object-fit: contain`; `npm run build`, `npx tsc --noEmit` и `git diff --check` прошли.
- Верхний заголовок и описание CRM-слайдера на главной заменены с общего текста про цифровые системы на `CRM-система ветеринарної клініки` и описание записей, пациентов, платежей и персонала; локализация UA/RU/EN сохранена через существующие ключи. `npm run build`, `npx tsc --noEmit` и `git diff --check` прошли.
- В CRM-блоке главной убраны дублирующие верхние title/description: вертикальное меню с названиями слайдов теперь начинается сверху и используется как основное меню блока, при этом переключение изображений и автоматическая смена сохранены. `npm run build`, `npx tsc --noEmit` и `git diff --check` прошли.
- Первый пункт CRM-меню «CRM-система ветеринарної клініки» увеличен до `16px` с line-height `22px`; остальные пункты сохранили `14px`. `npx tsc --noEmit` и `git diff --check` прошли.
- Все заголовки пунктов верхнего CRM-меню выровнены по размеру первого пункта: `16px` с line-height `22px`; описания пунктов не изменялись. `npx tsc --noEmit` и `git diff --check` прошли.
- В пунктах CRM-меню порядок изменён: сначала отображаются название и описание, затем нижняя полоса прогресса; верхняя полоса над названием убрана, автопереключение и активный progress-индикатор сохранены. `npx tsc --noEmit` и `git diff --check` прошли.
- На активных фото CRM-слайдера добавлены отдельные полупрозрачные glass-карточки с названием, действием и стрелкой; кликабельна только карточка, а не изображение. Все пункты главного меню ведут на `/portfolio?category=crm&project=vetscanct`, где `PortfolioCarousel` по query-параметрам сразу выбирает VetScanCT. Проверено в браузере: клик по фото URL не меняет, клик по карточке открывает CRM VetScanCT; `npm run build`, `npx tsc --noEmit`, `git diff --check` прошли.
- Форма glass-карточки изменена на длинную низкую pill-капсулу по референсу: ширина `620px`, высота `72px`, `border-radius: 999px`, blur и мягкое тёплое внутреннее свечение; мобильные размеры адаптированы. Browser-QA подтвердил `620×72px`, `backdrop-filter: blur(18px) saturate(1.15)`, радиус `999px`, изображение остаётся вне ссылки; `npm run build`, `npx tsc --noEmit`, `git diff --check` прошли.
- По уточнению пользователя glass-карточка уменьшена до компактной pill-формы: desktop максимум `480px × 56px`, mobile максимум `360px × 52px`; скругление, прозрачность и кликабельность сохранены. `npx tsc --noEmit` и `git diff --check` прошли.
- Ширина glass-карточки переведена на `max-content`: теперь она подстраивается под длину названия, но ограничивается отступами viewport на desktop/mobile, чтобы длинный текст не выходил за границы. `npx tsc --noEmit` и `git diff --check` прошли.
- Во второй пункт CRM-меню на главной подключён точный asset Beauty Master CRM `/assets/portfolio/beauty-master-crm.png`; подпись, описание и alt-текст используют существующую локализацию проекта. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Для второго пункта CRM-меню скриншот Beauty Master CRM переведён из `contain` в `cover`, поэтому изображение теперь заполняет всю ширину визуального блока без боковых пустых полей. `npx tsc --noEmit` и `git diff --check` прошли.
- В третий пункт CRM-меню на главной подключён точный asset спортивной CRM `/assets/portfolio/sports-crm.png`; подпись, описание и alt-текст используют локализованные данные спортивной CRM, а изображение заполняет ширину блока через `cover`. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Вокруг изображений CRM-слайдера добавлена контрастная glass-рамка: тонкая светлая граница, тёплый коралловый внешний акцент, внутренняя подсветка и глубокая мягкая тень; фото теперь визуально отделяется от тёмного фона. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Для светлого слайда Beauty Master CRM добавлена отдельная тёмно-синяя рамка с холодным голубым свечением и графитовой внутренней границей; рамки тёмных изображений не изменялись. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Цвет рамки светлого Beauty Master CRM заменён на контрастный кораллово-графитовый: внешняя линия стала ярче и толще, добавлены заметное коралловое свечение и тёмный внутренний контур, чтобы рамка не сливалась со скриншотом. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Коралловая рамка светлого Beauty Master CRM сделана тоньше: толщина уменьшена до `1px`, а свечение слегка ослаблено, чтобы сохранить контраст без визуальной тяжести. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Единая тонкая коралловая рамка включена для первых трёх CRM-фото в меню: VetScanCT, Beauty Master CRM и спортивной CRM; остальные слайды не изменялись. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- У коралловой рамки убрана белая верхняя полоса: внутренняя подсветка и псевдоэлемент теперь используют только слабый коралловый оттенок без белого блика. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- В четвёртый пункт CRM-меню на главной подключён точный asset Auto Service CRM `/assets/portfolio/auto-service-crm-promo-uk.png`; добавлены локализованные title, description и alt, а тонкая коралловая рамка сохранена. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Из CRM-меню на главной убраны три ненужных пункта: «Звіт про відповідність», «Агрегація портфелів» и «Листи клієнтам у вашому стилі». В меню остались первые четыре CRM-проекта. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- В первом пункте карточки «Вебсайти та лендинги» на главной подключён точный asset Luxury Travel `/assets/portfolio/luxury-travel.png`; title, description и alt-текст заменены на локализованные данные проекта. Остальные пункты карточки не изменялись. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Размер фото и рамки первого пункта «Вебсайти та лендинги» выровнен с CRM-слайдером: визуальная область теперь использует тот же компактный формат, при этом изображение продолжает заполнять рамку через `cover`. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Над CRM-слайдером на главной добавлен заголовок `CRM-системи` с теми же размерами и цветом, что и заголовок «Вебсайти та лендинги»; текст локализуется через существующий ключ категории CRM. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Для CRM-слайдера добавлен отдельный вертикальный отступ `34px` между заголовком «CRM-системи» и первым пунктом «CRM-система ветеринарної клініки»; карточка сайтов не изменялась. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Из карточки «Вебсайти та лендинги» удалено описание «Від лаконічних сайтів до складних вебзастосунків…»; заголовок и меню проектов сохранены. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- В карточке «Вебсайти та лендинги» добавлен такой же увеличенный вертикальный отступ между заголовком и первым пунктом меню, как в CRM-карточке. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Для первого фото Luxury Travel в карточке «Вебсайти та лендинги» задано позиционирование `object-position: center top`, чтобы верхняя часть скриншота полностью оставалась видимой внутри прежней рамки. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Рамка первого фото Luxury Travel переведена на тот же стиль `coralVisual`, что и рамки CRM-слайдера: одинаковые толщина, коралловый цвет, свечение и внутренний контур. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- В второй пункт карточки «Вебсайти та лендинги» добавлен присланный скрин VetScan CT `/assets/portfolio/vetscan-ua-promo.png`; файл скопирован без изменения, добавлены локализованные title, description и alt, рамка оставлена коралловой. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Для второго скрина VetScan CT включено позиционирование `object-position: center top`, чтобы верхняя часть изображения полностью оставалась видимой внутри рамки. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Для всех изображений карточки «Вебсайти та лендинги» добавлена такая же нижняя glass-карточка с названием проекта и стрелкой, как в CRM-слайдере; карточки без отдельного проекта ведут на общий `/portfolio`. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Карточки известных проектов получили точные ссылки на портфолио с категорией и project-id: VetScanCT CRM, Beauty Master CRM, спортивная CRM, Auto Service CRM, Luxury Travel и VetScan CT site; PortfolioCarousel сразу открывает соответствующий проект. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- При переходе по карточке с project-id PortfolioCarousel теперь автоматически прокручивается к центру выбранного блока, поэтому портфолио сразу показывает нужный проект, а не верхний hero-блок. `scroll-margin-top: 90px` учитывает фиксированную шапку. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Карточка «Спортивна CRM» теперь ведёт в категорию CRM на проект `sport-base-crm`, поэтому открывается экран Sport Base CRM `03 / 04`, как на втором скриншоте, вместо мобильной версии `sports-crm`. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- Все glass-карточки поверх изображений в `FeatureSlider` сделаны компактнее: уменьшены высота, внутренние отступы, расстояние между текстом и стрелкой, а также размер шрифта; для мобильной версии задан отдельный ещё более компактный размер.
- Фон всех glass-карточек `FeatureSlider` сделан немного прозрачнее, а blur усилен до `20px`, чтобы карточки сильнее выглядели как стекло при сохранении читаемости текста.
- В третье место меню «Вебсайти та лендинги» добавлен точный скрин `olive-oil-promo.png`; карточка использует проект `olive-oil` в категории `sites` и ведёт сразу в правильный раздел портфолио.
- В четвёртое место меню «Вебсайти та лендинги» добавлен точный скрин `childrens-party-promo.png`; карточка использует проект `childrens-party` в категории `sites` и ведёт сразу в правильное портфолио детских праздников.
- В пятое место меню «Вебсайти та лендинги» добавлен точный скрин `modular-house-promo.png`; карточка использует проект `modular-house` в категории `sites` и ведёт сразу в правильное портфолио модульных домов.
- В шестое место меню «Вебсайти та лендинги» добавлен точный скрин `idento-promo.png`; карточка использует проект `idento` в категории `sites` и ведёт сразу в правильное портфолио агентства.
- Из меню «Вебсайти та лендинги» убран пункт «Усе для незалежних фірм і більше»; шесть добавленных сайтов и их переходы в портфолио сохранены.
- В мобильном портфолио заменены ассеты спортивной карточки: preview теперь `fitness-mobile-app-ukr.png` (`2400×1500`), а центральное прокручиваемое изображение — `iphone-ukr-collage.png` (`2160×12898`); project-id `sports-crm` и место в категории `mobile` сохранены.
- В категорию «Мобільні застосунки» добавлена вторая карточка Air Scanner: preview `air-scanner-mobile-ad-ua-3d.png` (`1448×1086`), центральное прокручиваемое изображение `air-scanner-iphone-vertical-collage-v2.png` (`1282×4860`), project-id `air-scanner`.
- Для Air Scanner добавлена локализация изображений: в UA/RU остаются `air-scanner-mobile-ad-ua-3d.png` и `air-scanner-iphone-vertical-collage-v2.png`, а в EN автоматически подставляются `air-scanner-mobile-ad-en-3d.png` и `air-scanner-iphone-vertical-collage-batch2.png`.
- Центральные изображения в `PortfolioCarousel` слегка осветлены: `brightness` изменён с `0.72` на `0.82`; превью справа и изображения главной страницы не затронуты.
- Для спортивного мобильного приложения добавлена локализация изображений: в UA/RU остаются `fitness-mobile-app-ukr.png` и `iphone-ukr-collage.png`, а в EN автоматически подставляются `fitness-mobile-app-en.png` и `fitness-iphone-grid-english.png`.
- Название и описание спортивного мобильного приложения обновлены во всех языках: теперь явно указано, что это приложение для тренера и клиента, синхронизированное с CRM.
- В категорию «Сайти» портфоліо добавлен проект Diamant первым элементом: превью карточки использует `public/assets/portfolio/diamant-ukr-card.png` (`1600×1200`), а центральный прокручиваемый экран — `public/assets/portfolio/diamant-ukr-portfolio.png` (`2200×9390`). Название, описание и alt-текст добавлены для украинской, русской и английской локалей. `npx tsc --noEmit`, `git diff --check` и `npm run build` прошли.
- По уточнению пользователя Diamant переставлен в предпоследнюю позицию среди карточек категории «Сайты» — перед последним проектом `IDENTO`; содержимое карточки и изображения не менялись.
- Для английской локали Diamant получил отдельные изображения: карточка использует `public/assets/portfolio/diamant-natural-cosmetics-ad-en-3d.png`, а центральный просмотр — `public/assets/portfolio/diamant-website-screens-collage-en.png`. В украинской и русской локалях сохранены украинские ассеты.
- В категорию «Сайти» добавлен Boostify последней карточкой: превью использует `public/assets/portfolio/boostify-ukr-card.png` (`2000×1500`), а центральный прокручиваемый экран — `public/assets/portfolio/boostify-website-screens-collage.png` (`2200×5975`). Локализованы название, описание и alt-текст для UA/RU/EN.
- Для английской локали у Boostify заменено только превью карточки на `public/assets/portfolio/boostify-advertising-cover-en-strict-3d.png`; центральный экран `boostify-website-screens-collage.png` оставлен прежним.
- В раскрытых шагах `FooterProcess` увеличен `max-height` описания с `54px` до `110px`, чтобы полный текст не обрезался на мобильном экране и в локалях с более длинными фразами. Анимация раскрытия сохранена.
- На главной в слайдер «Вебсайти та лендинги» добавлен нижний пункт Diamant с фото `/assets/portfolio/diamant-ukr-card.png`. Фото получено из нового пользовательского пути и совпадает с уже существующим ассетом; glass-карточка ведёт на `/portfolio?category=sites&project=diamant`.
- На главной в слайдер «Вебсайти та лендинги» добавлен последний пункт Boostify с фото `/assets/portfolio/boostify-ukr-card.png`; ассет из нового пользовательского пути совпадает с уже существующим. Glass-карточка ведёт на `/portfolio?category=sites&project=boostify`.
- Для домашнего меню «Вебсайти та лендинги» добавлен отдельный прокручиваемый режим: видимая область рассчитана примерно на пять пунктов, остальные карточки доступны через вертикальный scroll. CRM- и mobile-слайдеры не изменялись.
- В английской локали `public/assets/portfolio/contra-crm-en.png` (`1448×1086`) подключён именно к карточке №02 «CRM-системи та внутрішні кабінети» в верхнем блоке возможностей на главной. Прежняя привязка к нижнему CRM-слайдеру удалена как неверная; украинская и русская версии верхней карточки не изменялись.
- Для английского CRM-скрина в карточке №02 задано `imagePosition: top`, чтобы изображение начиналось от верхнего края и верхняя часть макета полностью оставалась видимой.
- В карточке №03 «Мобільні застосунки» верхнего блока возможностей добавлена локализация изображений: UA/RU используют `public/assets/portfolio/medscanner-ukr.png`, EN — `public/assets/portfolio/medscanner-ad-en-3d.png`; для обоих задано выравнивание по верхнему краю.
- В портфолио CRM-проект `sport-base-crm` отделён от мобильного приложения: для него добавлены отдельные локализованные название, описание и alt-текст про CRM-систему для фитнес-зала, тренеров и клиентов. Мобильный проект `sports-crm` сохранён с отдельной мобильной формулировкой.
- В третьем пункте меню домашнего CRM-слайдера заменены title, description и alt с мобильного приложения на CRM-систему для фитнес-зала, тренеров и клиентов; ссылка по-прежнему ведёт на `sport-base-crm`. Мобильный пункт в отдельном слайдере портфолио не изменён.
- Для первого пункта домашнего CRM-слайдера в английской локали подключён `public/assets/portfolio/vet-clinic-crm-en.png` (`1448×1086`) с alt-текстом про VetScanCT; украинская и русская версии используют прежнее изображение.
- Для английской версии первого пункта домашнего CRM-слайдера изображение VetScanCT растянуто на всю ширину карточки и выровнено по верхнему краю (`imageFit: cover`, `imagePosition: top`); украинская и русская версии не изменялись.
